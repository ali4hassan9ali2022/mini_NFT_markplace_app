class TableModel {
  final String image, title, subTitle, count, rating;

  const TableModel({
    required this.image,
    required this.title,
    required this.subTitle,
    required this.count,
    required this.rating,
  });
}
